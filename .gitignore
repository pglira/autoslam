# KITTI dataset (user-populated, ~80 GB)
/data/dataset/

# Per-experiment build artifacts (rebuildable from build.sh)
experiments/*/slam
experiments/*/build/
experiments/*/target/
experiments/*/.cache/
experiments/*/Cargo.lock

# Per-experiment harness outputs (regenerated each run)
experiments/*/preds_dev/
experiments/*/preds_full/
experiments/*/preds_dev_recheck/

# Run-time logs (per build, per sequence)
/run_logs/

# Eval devkit build artifacts
eval/devkit/build/
eval/devkit/evaluate_odometry

# Lockfile + scratch
journal/.in_flight
/tmp_configs/
/run_logs/

# Local Claude tooling (skills, local settings) — not part of the project
.claude/

# Editor / OS noise
.vscode/
.idea/
.DS_Store
*.swp
*.swo
*~
