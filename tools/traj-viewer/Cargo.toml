[package]
name = "traj-viewer"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = { version = "0.28", default-features = false, features = ["default_fonts", "glow", "wayland", "x11"] }
egui = "0.28"
egui_plot = "0.28"

[profile.release]
lto = "thin"
codegen-units = 1
